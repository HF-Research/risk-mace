about_server <-
  function(id,
           user_age,
           is_data_valid,
           n_missing_inputs) {
    moduleServer(id,
                 function(input, output, session) {
                   ## Below is the module function
                 }
    )
  }
